# Pnyx public website

A dependency-free static landing page for `pnyx.dev`.

## Cloudflare Pages settings

- Production branch: `main`
- Framework preset: `None`
- Build command: `exit 0`
- Build output directory: `site`
- Root directory: `/`

Cloudflare Pages will publish every push to `main` and create preview deployments for pull requests.
